{
  "version": 2,
  "builds": [
    { 
      "src": "server.ts",
      "use": "@vercel/node"
    },
    { 
      "src": "api/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "dist/server.js" }
  ]
}