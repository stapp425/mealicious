{
  "builds": [
    { 
      "src": "server.ts",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "npm i && npm run dev"
      }
    },
    { "src": "api/**/*.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/$1" }
  ]
}